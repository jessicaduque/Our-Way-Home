using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Canvas : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void UIZed()
    {
        // Troca estilo hp
        // Desativa barra mana
        // Ativa barra stamina
        // Desativa EXP Amy
        // Ativa EXP próprio
        // Se tiver esquema de cores, desativa botão de troca Amy
        // Se tiver esquema de cores, ativa botão de troca próprio
        // Desativa pai dos botões de ataque Amy
        // Ativa pai dos botões de ataque próprio
    }

    public void UIAmy()
    {
        // Troca estilo hp
        // Ativa barra mana
        // Desativa barra stamina
        // Desativa EXP Zed
        // Ativa EXP próprio
        // Se tiver esquema de cores, desativa botão de troca Zed
        // Se tiver esquema de cores, ativa botão de troca próprio
        // Desativa pai dos botões de ataque Zed
        // Ativa pai dos botões de ataque próprio
    }
}
